<Page
    x:Class="RAGDataIngestionWPF.Views.SettingsPage"
    Style="{DynamicResource MahApps.Styles.Page}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:converters="clr-namespace:RAGDataIngestionWPF.Converters"
    xmlns:models="clr-namespace:RAGDataIngestionWPF.Models"
    xmlns:properties="clr-namespace:RAGDataIngestionWPF.Properties"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    mc:Ignorable="d" Background="DarkBlue"
    d:DesignHeight="450" d:DesignWidth="800">
    <Page.Resources>
        <converters:EnumToBooleanConverter x:Key="EnumToBooleanConverter" EnumType="models:AppTheme" />
    </Page.Resources>
    <ScrollViewer>
        <Grid Margin="{StaticResource SmallTopBottomMargin}">
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock
                Style="{StaticResource PageTitleStyle}"
                Text="{x:Static properties:Resources.SettingsPageTitle}" />
            <StackPanel
                x:Name="ContentArea"
                Grid.Row="1"
                Margin="{StaticResource MediumLeftRightMargin}">

                <TextBlock
                    Margin="{StaticResource SmallTopMargin}"
                    Style="{StaticResource SubtitleTextStyle}"
                    Text="{x:Static properties:Resources.SettingsPagePersonalizationTitle}" />
                <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
                    <TextBlock
                        Style="{StaticResource BodyTextStyle}"
                        Text="{x:Static properties:Resources.SettingsPageChooseThemeText}" />
                    <StackPanel Margin="{StaticResource XSmallTopMargin}">
                        <RadioButton
                            GroupName="AppTheme"
                            Content="{x:Static properties:Resources.SettingsPageRadioButtonLightTheme}"
                            FontSize="{StaticResource MediumFontSize}"
                            IsChecked="{Binding Theme, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding SetThemeCommand}" CommandParameter="Light" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </RadioButton>
                        <RadioButton
                            GroupName="AppTheme"
                            Content="{x:Static properties:Resources.SettingsPageRadioButtonDarkTheme}"
                            Margin="{StaticResource XSmallTopMargin}"
                            FontSize="{StaticResource MediumFontSize}"
                            IsChecked="{Binding Theme, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Checked">
                                    <i:InvokeCommandAction Command="{Binding SetThemeCommand}" CommandParameter="Dark" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </RadioButton>
                    </StackPanel>
                </StackPanel>

                <TextBlock
                    Style="{StaticResource BodyTextStyle}"
                    Text="ApplicationId" />
                <Grid Margin="{StaticResource XSmallTopMargin}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        Grid.Column="0"
                        IsReadOnly="True"
                        Text="{Binding ApplicationId, Mode=OneWay}" />
                    <Button
                        Grid.Column="1"
                        Margin="{StaticResource SmallLeftMargin}"
                        Command="{Binding RenewApplicationIdCommand}"
                        Content="Renew" />
                </Grid>




              
                <TextBlock
                    Margin="{StaticResource SmallTopMargin}"
                    Style="{StaticResource SubtitleTextStyle}"
                    Text="{Binding ChatHistoryTitleText, Mode=OneWay}" />
                <GroupBox Margin="{StaticResource SmallTopBottomMargin}" Header="{x:Static properties:Resources.SettingsPageChatHistoryGroupBoxTitle}">
                <StackPanel Margin="{StaticResource SmallTopMargin}">
                    <TextBlock Style="{StaticResource BodyTextStyle}" Text="{Binding ChatModelLabelText, Mode=OneWay}" />
                    <TextBox Margin="{StaticResource XSmallTopMargin}" Text="{Binding ChatModelName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Margin="{StaticResource SmallTopMargin}" Style="{StaticResource BodyTextStyle}" Text="{Binding EmbeddingsModelLabelText, Mode=OneWay}" />
                    <TextBox Margin="{StaticResource XSmallTopMargin}" Text="{Binding EmbeddingsModelName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Margin="{StaticResource SmallTopMargin}" Style="{StaticResource BodyTextStyle}" Text="{Binding ConnectionStringLabelText, Mode=OneWay}" />
                    <TextBox Margin="{StaticResource XSmallTopMargin}" MinHeight="64" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Text="{Binding ChatHistoryConnectionString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Margin="{StaticResource SmallTopMargin}" Style="{StaticResource BodyTextStyle}" Text="{Binding MaxContextMessagesLabelText, Mode=OneWay}" />
                    <TextBox Margin="{StaticResource XSmallTopMargin}" Text="{Binding MaxContextMessages, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Margin="{StaticResource SmallTopMargin}" Style="{StaticResource BodyTextStyle}" Text="{Binding MaxContextTokensLabelText, Mode=OneWay}" />
                    <TextBox Margin="{StaticResource XSmallTopMargin}" Text="{Binding MaxContextTokens, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <CheckBox Margin="{StaticResource SmallTopMargin}" Content="{Binding RagKnowledgeEnabledLabelText, Mode=OneWay}" IsChecked="{Binding RAGKnowledgeEnabled, Mode=TwoWay}" />
                    <CheckBox Margin="{StaticResource XSmallTopMargin}" Content="{Binding ChatHistoryContextEnabledLabelText, Mode=OneWay}" IsChecked="{Binding ChatHistoryContextEnabled, Mode=TwoWay}" />

                    <StackPanel Margin="{StaticResource SmallTopMargin}" Orientation="Horizontal">
                        <Button Content="{Binding SaveChatHistoryButtonText, Mode=OneWay}" Command="{Binding SaveChatHistorySettingsCommand}" />
                        <TextBlock Margin="{StaticResource SmallLeftMargin}" VerticalAlignment="Center" Style="{StaticResource BodyTextStyle}" Text="{Binding ChatHistorySettingsStatus, Mode=OneWay}" />
                    </StackPanel>
                </StackPanel>

                </GroupBox>


                <TextBlock
                    Text="{x:Static properties:Resources.SettingsPageUserTitle}"
                    Style="{StaticResource SubtitleTextStyle}" />

                <StackPanel Margin="{StaticResource SmallTopMargin}" Orientation="Horizontal">
                    <Ellipse Width="64" Height="64">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{Binding User.Photo}" Stretch="UniformToFill" />
                        </Ellipse.Fill>
                    </Ellipse>
                    <StackPanel Margin="{StaticResource SmallLeftMargin}">
                        <TextBlock
                            Text="{Binding User.Name}"
                            Style="{StaticResource BodyTextStyle}" />
                        <TextBlock
                            Text="{Binding User.UserPrincipalName}"
                            Margin="{StaticResource XSmallTopMargin}"
                            Style="{StaticResource BodyTextStyle}" />
                    </StackPanel>
                </StackPanel>
                <TextBlock Margin="{StaticResource SmallTopMargin}">
                    <Hyperlink Command="{Binding LogOutCommand}">
                        <TextBlock
                            Style="{StaticResource BodyTextStyle}"
                            Text="{x:Static properties:Resources.SettingsPageLogOutButtonText}" />
                    </Hyperlink>
                </TextBlock>
                <TextBlock
                    Margin="{StaticResource SmallTopMargin}"
                    Style="{StaticResource SubtitleTextStyle}"
                    Text="{x:Static properties:Resources.SettingsPageAboutTitle}" />
                <TextBlock
                    Text="{Binding VersionDescription, Mode=OneWay}"
                    Margin="{StaticResource XSmallTopMargin}"
                    Style="{StaticResource BodyTextStyle}" />
                <TextBlock
                    Margin="{StaticResource XSmallTopMargin}"
                    Style="{StaticResource BodyTextStyle}"
                    Text="{x:Static properties:Resources.SettingsPageAboutText}" />

                <TextBlock
                    Margin="{StaticResource SmallTopMargin}"
                    AutomationProperties.Name="{Binding Text, ElementName=settingsPrivacyStatement}">
                    <Hyperlink
                        Command="{Binding PrivacyStatementCommand}"
                        AutomationProperties.Name="{Binding Text, ElementName=settingsPrivacyStatement}">
                        <TextBlock
                            x:Name="settingsPrivacyStatement"
                            Style="{StaticResource BodyTextStyle}"
                            Text="{x:Static properties:Resources.SettingsPagePrivacyStatementText}" />
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>